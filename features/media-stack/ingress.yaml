apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: media-stack-ingress
spec:
  entryPoints:
  - websecure
  routes:
  - match: Host(`bazarr.ts.noodoodle.us`)
    services:
    - name: bazarr-web
      namespace: media-stack
      port: 80
  - match: Host(`prowlarr.ts.noodoodle.us`)
    services:
    - name: prowlarr-web
      namespace: media-stack
      port: 80
  - match: Host(`radarr.ts.noodoodle.us`)
    services:
    - name: radarr-web
      namespace: media-stack
      port: 80
  - match: Host(`sonarr.ts.noodoodle.us`)
    services:
    - name: sonarr-web
      namespace: media-stack
      port: 80
