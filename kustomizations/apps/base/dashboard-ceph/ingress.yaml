apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ceph-ingress
  namespace: rook-ceph
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: pihole-web
      port:
        name: http
  tls:
    - hosts:
        - pihole
