apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pihole-ingress
  namespace: pihole
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: pihole-web
      port:
        name: http
  tls:
    - hosts:
        - pihole
