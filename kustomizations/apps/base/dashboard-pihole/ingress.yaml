apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: pihole-ingress
  ingress: local
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`pihole.noodoodle.us`)
      services:
        - name: pihole-web
          port: 80
