apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: traefik-ingress
  labels:
    ingress: local
spec:
  entryPoints:
    - websecure
  routes:
  - match: Host(`traefik.noodoodle.us`)
    services:
    - name: api@internal
      kind: TraefikService
